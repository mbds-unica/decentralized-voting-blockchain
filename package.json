{
  "name": "decentralized-voting-blockchain",
  "private": true,
  "version": "1.0.0",
  "description": "Decentralized voting dApp on Sepolia",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify:sepolia": "hardhat run scripts/verify.js --network sepolia",
    "smoke:sepolia": "hardhat run scripts/smokeCheck.js --network sepolia",
    "seed:sepolia": "hardhat run scripts/seedProposals.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
